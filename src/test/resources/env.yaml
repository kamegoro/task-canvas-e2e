taskCanvasWeb:
  backendHost: localhost
  backendPort: 8080
taskCanvas:
  rest:
    baseUrl: "http://localhost:8080"
  db:
    jdbcUrl: "jdbc:postgresql://localhost:10080/task_canvas"
    username: "developer"
    password: "developer"
    schema: "task_canvas"
taskCanvasTagManager:
  rest:
    baseUrl: "http://localhost:9090"
